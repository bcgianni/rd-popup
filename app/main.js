var Popup = require('./generate-popup.js');

var RDPopup = ( function(window, undefined) {
  
  function RDPopup(url, time) {

    this.init = function() {
      alert(Popup);
    };
  }

  return RDPopup;

})(window);